version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: bundle exec rails s -e production
    volumes:
      - .:/rails_tutorial_docker
    tty: true
    stdin_open: true

volumes:
  db-volume:
